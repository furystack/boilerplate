{
  "name": "service",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "GPL-2.0-only",
  "private": true,
  "scripts": {
    "start": "yarn build && node ./dist/service.js",
    "seed": "yarn build && node ./dist/seed.js",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^17.0.31"
  },
  "dependencies": {
    "@furystack/core": "^11.0.1",
    "@furystack/filesystem-store": "^3.0.1",
    "@furystack/inject": "^6.0.1",
    "@furystack/logging": "^3.0.1",
    "@furystack/repository": "^6.0.1",
    "@furystack/rest-service": "^6.0.1",
    "tslib": "^2.4.0"
  }
}
