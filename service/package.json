{
  "name": "service",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "GPL-2.0-only",
  "private": true,
  "scripts": {
    "start": "yarn build && node ./dist/service.js",
    "seed": "yarn build && node ./dist/seed.js",
    "write-metadata": "yarn build && node ./dist/write-metadata.js",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "@types/node": "^13.11.0",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "@furystack/core": "^8.0.11",
    "@furystack/inject": "^4.0.24",
    "@furystack/logging": "^2.0.31",
    "@furystack/repository": "^2.0.38",
    "@furystack/rest": "^1.1.8",
    "@furystack/rest-service": "^1.1.11",
    "common": "1.0.0",
    "tslib": "^1.11.1"
  }
}
