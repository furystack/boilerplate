{
  "name": "service",
  "version": "1.0.1",
  "main": "dist/index.js",
  "license": "GPL-2.0-only",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "yarn build && node ./dist/service.js",
    "seed": "yarn build && node ./dist/seed.js",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "typescript": "^5.0.4",
    "vitest": "^0.31.4"
  },
  "dependencies": {
    "@furystack/core": "^12.0.4",
    "@furystack/filesystem-store": "^4.0.4",
    "@furystack/inject": "^8.0.4",
    "@furystack/logging": "^4.0.4",
    "@furystack/repository": "^7.0.4",
    "@furystack/rest-service": "^7.0.5",
    "@furystack/security": "^3.0.4",
    "common": "workspace:^"
  }
}
