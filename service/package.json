{
  "name": "service",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "GPL-2.0-only",
  "private": true,
  "scripts": {
    "start": "yarn build && node ./dist/service.js",
    "seed": "yarn build && node ./dist/seed.js",
    "write-metadata": "yarn build && node ./dist/write-metadata.js",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^14.0.14"
  },
  "dependencies": {
    "@furystack/core": "^9.1.4",
    "@furystack/inject": "^4.0.33",
    "@furystack/logging": "^2.0.40",
    "@furystack/odata": "^2.1.11",
    "@furystack/repository": "^3.0.7",
    "@furystack/rest-service": "^2.1.11",
    "tslib": "^2.0.0"
  }
}
