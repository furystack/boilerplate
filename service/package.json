{
  "name": "service",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "GPL-2.0-only",
  "private": true,
  "scripts": {
    "start": "yarn build && node ./dist/service.js",
    "seed": "yarn build && node ./dist/seed.js",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^15.6.0"
  },
  "dependencies": {
    "@furystack/core": "^10.0.6",
    "@furystack/filesystem-store": "^2.0.6",
    "@furystack/inject": "^4.0.65",
    "@furystack/logging": "^2.0.72",
    "@furystack/repository": "^5.0.6",
    "@furystack/rest-service": "^4.0.6",
    "tslib": "^2.2.0"
  }
}
