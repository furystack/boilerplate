{
  "name": "service",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "GPL-2.0-only",
  "private": true,
  "scripts": {
    "start": "yarn build && node ./dist/service.js",
    "seed": "yarn build && node ./dist/seed.js",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^15.12.4"
  },
  "dependencies": {
    "@furystack/core": "^10.0.10",
    "@furystack/filesystem-store": "^2.0.10",
    "@furystack/inject": "^4.0.69",
    "@furystack/logging": "^2.0.76",
    "@furystack/repository": "^5.0.10",
    "@furystack/rest-service": "^4.0.10",
    "tslib": "^2.3.0"
  }
}
