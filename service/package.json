{
  "name": "service",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "GPL-2.0-only",
  "private": true,
  "scripts": {
    "start": "yarn build && node ./dist/service.js",
    "seed": "yarn build && node ./dist/seed.js",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^17.0.35"
  },
  "dependencies": {
    "@furystack/core": "^11.0.3",
    "@furystack/filesystem-store": "^3.0.3",
    "@furystack/inject": "^6.0.3",
    "@furystack/logging": "^3.0.3",
    "@furystack/repository": "^6.0.3",
    "@furystack/rest-service": "^6.0.4",
    "tslib": "^2.4.0"
  }
}
