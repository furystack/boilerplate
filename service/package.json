{
  "name": "service",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "GPL-2.0-only",
  "private": true,
  "scripts": {
    "start": "yarn build && node ./dist/service.js",
    "seed": "yarn build && node ./dist/seed.js",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^16.11.12"
  },
  "dependencies": {
    "@furystack/core": "^10.4.1",
    "@furystack/filesystem-store": "^2.0.25",
    "@furystack/inject": "^5.0.0",
    "@furystack/logging": "^2.0.84",
    "@furystack/repository": "^5.0.25",
    "@furystack/rest-service": "^4.1.5",
    "tslib": "^2.3.1"
  }
}
