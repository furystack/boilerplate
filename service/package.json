{
  "name": "service",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "GPL-2.0-only",
  "private": true,
  "scripts": {
    "start": "yarn build && node ./dist/service.js",
    "seed": "yarn build && node ./dist/seed.js",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^14.10.1"
  },
  "dependencies": {
    "@furystack/core": "^9.1.9",
    "@furystack/filesystem-store": "^1.1.9",
    "@furystack/inject": "^4.0.38",
    "@furystack/logging": "^2.0.45",
    "@furystack/odata": "^2.1.11",
    "@furystack/repository": "^3.0.12",
    "@furystack/rest-service": "^2.2.2",
    "tslib": "^2.0.1"
  }
}
