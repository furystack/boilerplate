{
  "name": "service",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "GPL-2.0-only",
  "private": true,
  "scripts": {
    "start": "yarn build && node ./dist/service.js",
    "seed": "yarn build && node ./dist/seed.js",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^18.0.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@furystack/core": "^11.0.5",
    "@furystack/filesystem-store": "^3.0.5",
    "@furystack/inject": "^7.0.1",
    "@furystack/logging": "^3.0.5",
    "@furystack/repository": "^6.0.5",
    "@furystack/rest-service": "^6.0.6",
    "@furystack/security": "^2.0.5",
    "@types/jest": "^28.1.4",
    "common": "workspace:^",
    "tslib": "^2.4.0"
  }
}
