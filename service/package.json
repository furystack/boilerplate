{
  "name": "service",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "GPL-2.0-only",
  "private": true,
  "scripts": {
    "start": "yarn build && node ./dist/service.js",
    "seed": "yarn build && node ./dist/seed.js",
    "write-metadata": "yarn build && node ./dist/write-metadata.js",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^13.1.6"
  },
  "dependencies": {
    "@furystack/core": "^7.0.1",
    "@furystack/inject": "^4.0.4",
    "@furystack/logging": "^2.0.10",
    "@furystack/odata": "^2.1.11",
    "@furystack/repository": "^2.0.13",
    "@furystack/rest-service": "^1.0.1",
    "tslib": "^1.10.0"
  }
}
