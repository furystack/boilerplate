{
  "name": "service",
  "version": "1.0.1",
  "main": "dist/index.js",
  "license": "GPL-2.0-only",
  "private": true,
  "scripts": {
    "start": "yarn build && node ./dist/service.js",
    "seed": "yarn build && node ./dist/seed.js",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@furystack/core": "^11.1.0",
    "@furystack/filesystem-store": "^3.1.0",
    "@furystack/inject": "^7.1.0",
    "@furystack/logging": "^3.1.0",
    "@furystack/repository": "^6.1.0",
    "@furystack/rest-service": "^6.2.1",
    "@furystack/security": "^2.1.0",
    "@types/jest": "^29.2.1",
    "common": "workspace:^"
  }
}
